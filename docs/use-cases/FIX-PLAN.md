# FIX-PLAN — Kế hoạch sửa theo Use Case

Tổng hợp từ mục **"Đối chiếu code"** trong `docs/use-cases/*.md` (audit ngày 2026-06-10).
Chỉ liệt kê case **CÓ sai lệch** (🔴/🟡 hoặc 🟢 nhưng có sai lệch hành vi/wording). Các case 🟢 "Đạt" thuần (chỉ thiếu test không nghiêm trọng) **không** đưa vào đây.

> Quy ước: **Mức rủi ro** = Cao / Trung bình / Thấp · **Ước lượng** = nhỏ / vừa / lớn.
> ⚖️ = quyết định nghiệp vụ/thiết kế — **đã chốt** (xem mục "Quyết định đã chốt").

---

## Quyết định đã chốt (PO — 2026-06-10)

### Nghiệp vụ lớn (đã hỏi & chốt)

1. ~~**AFF-03 — Hoa hồng CTV:** tính theo **đơn GIAO THÀNH CÔNG (gồm COD)**~~ ✅ **(B2 done)** → event `OrderDelivered` fire khi `status→delivered` (API admin / admin web / webhook VTP), dời mốc ghi `RecordAffiliateCommission` sang đó; giữ clawback.
2. **ORDER-06 — Kho đơn online chưa trả:** **Auto-cancel + hoàn kho sau timeout** — job tự huỷ đơn `pending` online & gọi `releaseReservation` sau ~15–20 phút chưa trả. **Không** đổi sang 2-phase reserve.
3. **ORDER-04 — BANK:** **Tách BANK khỏi `isOfflineFlow` NGAY** (mở SDK, chờ `PaymentDone` như MoMo) để sẵn sàng production.
4. **HUB-01 — Dashboard farm:** **Tách riêng 2 chỉ số** "đã đặt (hôm nay)" và "đã giao (hôm nay)" hiển thị song song; mỗi chỉ số tự nhất quán basis.
5. **PROD-05 — SP hết hàng:** **Hiện trong list kèm badge "Hết hàng"** (list/search/home/category đổi sang `allProductsState` + badge, nút thêm giỏ disabled). *(thay vì ẩn hoàn toàn)*
6. **ORDER-03 — Nhãn trạng thái đơn (khách):** **Thêm nhãn chữ** map 6 status BE → tiếng Việt ở `detail` + list đơn khách.
7. **ROLE-05 — Tạm dừng farm:** tạo **1 message "tạm dừng" chung** (vd "Farm của bạn đang tạm dừng, vui lòng liên hệ admin") áp cho cả `is_active=false` lẫn `farm_partner_status='suspended'`.

### Mục nhỏ — default đã áp (báo trước, override sau nếu cần)

- **ORDPRO-11:** thêm rule BE `reason: required_if:reason_code,other|min:5`.
- **ORDPRO-09/10:** nhãn hoàn tiền sau-huỷ truyền theo `payment_method` (ZaloPay 5–15′ / MoMo ~24h / Bank 2–7 ngày); giữ nguyên các mốc SLA hiện có, bỏ con số cứng dùng chung.
- **STOCK-04:** thêm cap số lượng client theo `stockAvailable` ở stepper/`QuantityInput` (vẫn giữ chốt BE 422).
- **AFF-04:** `updateBank` coi field **vắng mặt** trong request là "không đổi" (chỉ ghi khi key tồn tại); gửi `""` mới = xoá.
- **PROD-04:** **giữ nguyên** hành vi "khách thấy SP khi tồn>0 (không lọc farm)"; chỉ ghi rõ chuỗi đủ điều kiện = tạo SP → gắn farm → **nhập lô (batch active)**. Không đổi logic API.
- **ROLE-01/02:** thay silent-redirect bằng thông báo "bị chặn"; phân biệt `requested` → hiện "đang chờ duyệt" thay vì đẩy lại form đăng ký.
- **AUTH-03:** thống nhất 1 placeholder tên (**"Khách Zalo"**) ở cả FE lẫn nhánh tạo customer BE.

---

## 1) Bốn case lỗi đã ghi nhận (ưu tiên cao nhất)

| Mã case | Sai lệch chính | File liên quan | Mức rủi ro | Ước lượng |
|---|---|---|---|---|
| **AUTH-01** | Lần đầu cấp quyền xong tên/ảnh chưa hiện ngay: `useInitAuth` chạy 1 lần lúc mount; `refreshPermissions` chỉ bump key, KHÔNG re-`getUserInfo` cũng KHÔNG re-`authenticate` → tên/ảnh thật chỉ ra ở lần mở thứ 2. | FE `src/hooks.ts` (`useInitAuth`, `useRequestInformation`, `refreshPermissions`); BE `ZaloApiController::authenticate` | Trung bình | vừa |
| **PROD-02** ✅ | ~~Báo lỗi "tên bắt buộc" ra **tiếng Anh** ("The name field is required.") do thiếu `lang/vi/validation.php`~~ → **Đã sửa (B3):** tạo `resources/lang/vi/validation.php` + `attributes` Việt hoá → message "tên sản phẩm là bắt buộc". | BE `resources/lang/vi/validation.php` (mới); `ZaloProductController@store` | Thấp | ✅ |
| **ORDER-10** | Code đúng (message + chặn ở validate/checkout) nhưng repo **KHÔNG có seeder voucher** → `GIAM20K`/`SALE10`/`FREESHIP` không tồn tại → manual test fail vì DATA, không phải code. | BE thiếu seeder trong `database/seeders`; `Voucher::isUsable` | Trung bình | nhỏ |
| **ORDER-06** ✅ | ~~Đơn online bỏ dở **GIỮ kho**; **BANK gộp luồng offline**; `resultCode` mặc định = 1 (success) khi thiếu field~~ → **Đã sửa (B1):** job `CancelUnpaidOrder` tự huỷ + hoàn kho sau ~20′; tách BANK; chặn default-success + race guard. | BE `ZaloApiController` (`checkout`, `notifySDK`), `Jobs/CancelUnpaidOrder` (mới), `Jobs/CheckPaymentStatus`; FE `hooks.ts` `useCheckout` | **Cao** | lớn |

---

## 2) Nhóm ROLE — phân quyền (rủi ro bảo mật)

| Mã case | Sai lệch chính | File liên quan | Mức rủi ro | Ước lượng |
|---|---|---|---|---|
| **ROLE-04** ✅ | ~~🔴 Staff VẪN thấy tab "Thu nhập" và VẪN gọi được `/farm/payouts` + `/farm/payouts/{id}`~~ → **Đã sửa (B7)**: BE gate `ensureOwner()` (403 nếu staff) + FE ẩn tab/màn chặn khi `!is_owner`. | FE `components/footer.tsx` (`FARM_NAV`), `pages/farm/payouts.tsx`; BE `FarmHubController::payouts/payoutDetail` | **Cao** | vừa |
| **ROLE-01** | Khách cố mở `/farm*` bị **âm thầm redirect** `/farm/register` thay vì thông báo "bị chặn". | FE `components/layout.tsx:48-54` | Thấp | nhỏ |
| **ROLE-02** | KH-REQ (đã đăng ký, **chờ duyệt**) bị đẩy lại form `/farm/register`; FE không phân biệt trạng thái `requested` để hiện "đang chờ duyệt" (BE message đúng). | FE `components/layout.tsx`; BE `Customer::isFarmPartner` | Thấp | nhỏ–vừa |
| **ROLE-05** ✅ | 2 đường tạm dừng cho **2 message khác nhau**: farm `is_active=false` (đúng kỳ vọng) vs `farm_partner_status='suspended'` ("không có quyền…") → UI không nhất quán. **→ Chốt: 1 message "tạm dừng" chung cho cả 2 đường.** | BE `EnsureFarmPartner.php`, `Customer::isFarmPartner` | Thấp | nhỏ |

---

## 3) Nhóm ORDER / ORDPRO — luồng tiền

| Mã case | Sai lệch chính | File liên quan | Mức rủi ro | Ước lượng |
|---|---|---|---|---|
| **ORDER-04** ✅ | ~~`BANK_SANDBOX` ∈ `isOfflineFlow` → hiện "Đặt hàng thành công" ngay~~ → **Đã sửa (B1):** `isOfflineFlow` chỉ còn `startsWith("COD")`; BANK chờ `PaymentDone` như MoMo (production-ready). | FE `hooks.ts` `useCheckout` (`isOfflineFlow`) | **Cao** | vừa |
| **ORDER-08** | Thiếu test idempotency 2×`/checkout`; **TOCTOU**: `Cache::put` chỉ chạy SAU khi tạo đơn → 2 request đồng thời lý thuyết vẫn lọt 2 đơn (FE `inFlightRef` chặn double-tap thực tế). | BE `ZaloApiController:175-206,448`; FE `useCheckout` | Trung bình | nhỏ |
| **ORDER-16** | Thiếu test nhánh **listener** `CreateVtpOrderOnPayment` (đơn online shipping tạo VTP qua event); test hiện chỉ phủ COD-inline. | BE `Listeners/CreateVtpOrderOnPayment` | Thấp | nhỏ |
| **ORDER-03** ✅ | Không có **nhãn chữ "Chờ xác nhận"** phía khách (chuỗi chỉ ở `farm/orders.tsx`); `detail.tsx` không render label trạng thái đơn dạng chữ cho khách. **→ Chốt: thêm nhãn trạng thái đơn (map 6 status → tiếng Việt).** | FE `pages/orders/detail.tsx`, `order-summary.tsx` | Thấp | nhỏ–vừa |
| **ORDPRO-04** ✅ B4 | ~~Guard chặn lùi status **có code** nhưng **thiếu test** 422 "Không thể chuyển đơn từ …"~~ → **B4:** phủ test admin web (`AdminWebOrderUpdateGuardTest`) + API (assert message vào `UpdateOrderStatusTest`). Guard không đổi. | BE `ZaloOrderController@update`, `ZaloApiController@updateStatus` | Thấp | ✅ |
| **ORDPRO-05** ✅ B4 | ~~Guard chặn huỷ đơn `delivered` **có code** nhưng **thiếu test** 422 "Không thể hủy đơn hàng đã giao thành công" (admin/API/khách)~~ → **B4:** test cả 3 đường (admin web/API/khách `CustomerCancelGuardTest`). | BE `ZaloOrderController@update`, `ZaloApiController@updateStatus`, `cancelByCustomer` | Thấp | ✅ |
| **ORDPRO-08** ✅ | ~~Thiếu test `refund_status='not_required'` **và** hoàn kho sau huỷ khách (COD)~~ → **B6** (kho+not_required) + **B5** (assert COD không gọi refund API). | BE `cancelByCustomer`, `RefundService`, `StockService::releaseReservation` | Trung bình | ✅ |
| **ORDPRO-09** ✅ | ~~Message "5–15 phút" tĩnh; refund API fail → `pending_manual` nhưng label hiện "2–7 ngày" (mâu thuẫn)~~ → **B5**: nhãn sau-huỷ `detail.tsx` key theo `payment_method`+`refundStatus` (ZaloPay fail → "đang xử lý thủ công"); + test nhánh ZALOPAY/job. | FE `detail.tsx` (`refundStatusInfo`); BE test `RefundServiceTest`/`CheckRefundStatusJobTest` | Trung bình | ✅ |
| **ORDPRO-10** ✅ | ~~Nhãn sau-huỷ **luôn "(2–7 ngày)"** cho cả MoMo lẫn Bank~~ → **B5**: `pending_manual` theo method (MoMo ~24h / Bank 2–7 ngày), khớp modal; + test MOMO/BANK. | FE `detail.tsx` (`refundStatusInfo`); BE test `RefundServiceTest` | Trung bình | ✅ |
| **ORDPRO-11** ✅ B4 | ~~Ràng buộc lý do ≥5 ký tự **chỉ ở FE**; gọi thẳng API vẫn huỷ với `reason:'a'` (BE chỉ `nullable|max:500`, không có `min:5`)~~ → **B4:** thêm rule BE `reason: required_if:reason_code,other|min:5|max:500` + test `CustomerCancelGuardTest`. | BE `cancelByCustomer` | Thấp | ✅ |

---

## 4) Còn lại (xếp theo rủi ro giảm dần)

| Mã case | Sai lệch chính | File liên quan | Mức rủi ro | Ước lượng |
|---|---|---|---|---|
| ~~**AFF-03**~~ ✅ B2 | ~~🔴 **Lệch nghiệp vụ:** hoa hồng chỉ sinh khi **thanh toán ONLINE thành công** — đơn **COD KHÔNG BAO GIỜ** sinh hoa hồng~~ → **Đã sửa (B2):** dời mốc ghi commission sang event `OrderDelivered` (fire tại API/admin-web/VTP-webhook khi `status→delivered`), áp dụng mọi method gồm COD; giữ clawback. | BE `Events/OrderDelivered`, `Listeners/RecordAffiliateCommission`, `EventServiceProvider`, `ZaloApiController::updateStatus`, `Admin/ZaloOrderController::update`, `VtpWebhookService` | **Cao** | ✅ |
| **AFF-02** ✅ B19 | ~~Rủi ro Zalo **strip query `?ref=`** khi mở từ share link → không bắt được mã giới thiệu~~ → **Đã sửa (B19):** bắt mã từ **`getRouteParams()`** (nguồn chính ZMP, sống sót URL-rewrite `/zapps/`) + fallback `?ref=`, gom `captureRefCode()` first-capture-wins; guard `applyPendingReferral` apply 1 lần/phiên (chống 5 điểm gọi trùng), giữ retry khi lỗi mạng. Không ghi đè referrer cũ (BE 409). | FE `src/app.ts`, `utils/affiliate.ts` | Trung bình | ✅ |
| ~~**HUB-01**~~ ✅ B14 | ~~Dashboard "hôm nay" tính theo đơn **ĐÃ GIAO** (`delivered_at`), không gồm đơn vừa đặt; card "Đã bán" (delivered) vs list "Sản phẩm hôm nay" (`delivering,delivered` + `created_at`) **khác basis** → 2 con số lệch.~~ → **Đã sửa (B14):** tách 2 chỉ số "Đã đặt hôm nay" (`created_at`, mọi status trừ cancelled) / "Đã giao hôm nay" (`delivered_at`, delivered), mỗi chỉ số tự nhất quán card+list. FE 2 tab Đặt/Giao. **+B18:** sửa lỗi lệch 7h "hôm nay" (query so giờ VN trực tiếp, bỏ `setTimezone('UTC')`/`CONVERT_TZ`). | BE `FarmDashboardService` (`placedBaseQuery`/`aggregateMetrics`), `FarmHubController@overview/productsToday` | Trung bình | ✅ |
| **STOCK-06** | Code hoàn kho đúng nhưng test **cố tình bỏ** assert hoàn kho (`ViettelPostWebhookTest:204-206`) → không test nào khẳng định `quantity_sold/remaining` hoàn đúng + `depleted→active` sau cancel. | BE `StockService::releaseReservation`; tests | Trung bình | nhỏ–vừa |
| **STOCK-02** | FE **không có date picker** hạn sử dụng ở mọi màn nhập (BE đã nhận `expire_date`); không màn nào hiển thị `expire_date` của lô đã lưu (list/movements là SKU-aggregate). | FE `pages/farm/stock-in.tsx`, `import-sheet.tsx` (BE `importBatch` đã hỗ trợ) | Trung bình | vừa |
| **PROD-01** | ID gán thủ công `max(id)+1` (`$incrementing=false`) → **race condition** 2 admin tạo đồng thời trùng id; flash `'Product created'` tiếng Anh; field danh mục/đơn vị/ảnh **không** `required`. | BE `ZaloProductController@store` | Trung bình | vừa |
| **PACK-07** | FE list pickup hiện chuỗi cứng " · Pickup" **chưa hiện tên trạm**; `show()` có `station_name` nhưng **chưa wire route** `/farm/orders/:id`. (Che SĐT/địa chỉ ĐẠT.) | FE `pages/farm/orders.tsx`; BE `FarmPackingController@show` | Trung bình | vừa |
| **STOCK-04** | FE **không chặn sớm** số lượng theo tồn (chỉ chặn ở checkout 422); FE **viết lại** message thay vì nguyên văn BE. | FE `pages/catalog/product-detail.tsx`, `hooks.ts` `useCheckout` | Thấp | nhỏ–vừa |
| **PROD-03** | Message ảnh sai định dạng/>2MB ra **tiếng Anh**; nhánh `processImage` ném `\Exception` thô → rủi ro **500** (không guard hiển thị). Thiếu test upload. | BE `ZaloProductController@store/@processImage`; `lang/vi` | Thấp | nhỏ |
| **PROD-04** ✅ | "Gắn farm" **một mình KHÔNG đủ** để khách thấy SP: API `/products` không lọc farm, SP hiện khi tồn>0; SP đã gắn farm nhưng chưa nhập kho → `stock_available=0` → bị ẩn. **→ Chốt: giữ nguyên (visibility = tồn>0), chỉ ghi rõ điều kiện.** | BE `ZaloApiController@products`; `FarmController@attachProduct` | Thấp | nhỏ |
| **PROD-05** ✅ | SP hết hàng bị **ẩn hoàn toàn** khỏi list/search/home/category (`productsState` filter), không hiện badge "Hết hàng" — chỉ thấy nhãn khi vào thẳng product-detail. **→ Chốt: hiện trong list kèm badge "Hết hàng".** | FE `src/state.ts` `productsState`; `pages/catalog` | Thấp | vừa |
| **AFF-04** | `updateBank` gửi `""` **vẫn ghi đè thành ""** (không phân biệt "không đổi" vs "xoá"). Thiếu test `referrals`/`bank`/`me` (shape, mask, phân trang). | BE `AffiliateController@updateBank:129-150`, `@referrals`; FE `bank-info-form.tsx` | Thấp | nhỏ |
| **AFF-05** | Toàn bộ admin-web `AffiliatePartnerController` (approve/reject, payout FIFO, toggle settings) **KHÔNG có test**. Code khớp kỳ vọng. | BE `Admin/AffiliatePartnerController` | Thấp | vừa |
| **AUTH-03** | Placeholder lệch: FE mặc định **"Khách Zalo"** vs BE **"Zalo User"** → có thể nháy đổi tên. (Không lỗi đỏ.) | FE `src/state.ts` `userInfoState`; BE `authenticate` | Thấp | nhỏ |

---

## Nhóm batch — sửa chung 1 chỗ code

Gộp các case động vào cùng file/lớp để sửa + viết test 1 lần (thứ tự đề xuất theo độ ưu tiên & phụ thuộc).

| # | Batch (chỗ code chung) | Case gộp | Ghi chú |
|---|---|---|---|
| **B1** ✅ | **Vòng đời thanh toán & giữ kho** — `ZaloApiController` (`checkout`/`notifySDK`) + `Jobs/CancelUnpaidOrder` (mới) + `Jobs/CheckPaymentStatus` + FE `useCheckout` (`isOfflineFlow`) | **ORDER-06** ✅, **ORDER-04** ✅ | **Done:** job mới `CancelUnpaidOrder` tự huỷ + `releaseReservation` đơn online `pending` sau ~20′ (`ZALO_UNPAID_TIMEOUT_MINUTES`), best-effort poll Zalo trước khi huỷ; dispatch từ `checkout` (phủ cả khi đóng cổng trước `/link`); `notifySDK` chặn default-success (thiếu/sai `resultCode` → KHÔNG paid) + race guard `status='cancelled'`; `CheckPaymentStatus` thêm guard cancelled; FE tách BANK khỏi `isOfflineFlow`. Test: khôi phục toàn bộ suite `Zalo` (`composer test:zalo` 34 xanh) + `CancelUnpaidOrderTest`. |
| **B2** ✅ | **Sự kiện hoa hồng** — `Events/OrderDelivered` (mới) + `Listeners/RecordAffiliateCommission` + `EventServiceProvider` + 3 điểm fire (`ZaloApiController::updateStatus`, `Admin/ZaloOrderController::update`, `VtpWebhookService`) | **AFF-03** ✅ | **Done:** hoa hồng theo **đơn giao thành công (gồm COD)** — tạo event `OrderDelivered`, fire tại MỌI điểm `status→delivered` (API admin / admin web / webhook VTP 501); GỠ `RecordAffiliateCommission` khỏi `OrderPaymentSucceeded` (giữ DeductStock/CreateVtp/SendNotification ở mốc paid), gắn vào `OrderDelivered`; idempotent `firstOrCreate`; giữ clawback. Test: `CommissionOnDeliveryTest` (6) + sửa `CommissionCreditedOnPaymentTest`/`AffiliateCommissionCalculationTest`; `composer test:affiliate` 33 xanh. |
| **B3** ✅ | **i18n validation tiếng Việt** — `resources/lang/vi/validation.php` (+ `attributes`) + `ZaloProductController@store` | **PROD-02** ✅, **PROD-03** ✅, **PROD-01** ✅ | **Done (2026-06-11):** tạo `lang/vi/validation.php` (dịch đầy đủ + `attributes` Việt hoá tên field) → PROD-02 "tên sản phẩm là bắt buộc", PROD-03 message ảnh tiếng Việt; bọc `processImage` trong try/catch → lỗi xử lý ảnh trả `ValidationException` VN, **KHÔNG** 500. PROD-01: bỏ `max(id)+1` thô → `DB::transaction`+`lockForUpdate` (atomic, **giữ `$incrementing=false`**, không đổi schema/migration vì mock id cố định khớp FE); `category_id`/`unit_id`/`image` → `required`; flash → "Đã tạo sản phẩm". Test: `AdminProductCreateTest` (7 case: required-name VN, ảnh sai-định-dạng/quá-2MB VN, corrupt-image-no-500, 2 SP id khác nhau, required danh mục, flash VN) xanh; regression `composer test:feature` các suite liên quan xanh (`OrderCreationWithShippingTest` fail **pre-existing**, không liên quan B3 — xác nhận bằng clean-tree run). FE blade chưa đụng (HTML `required` để ngoài scope). |
| **B4** ✅ | **Guard & validate trạng thái/huỷ đơn** — `ZaloOrderController@update` + `ZaloApiController@updateStatus` + `cancelByCustomer` | **ORDPRO-04** ✅, **ORDPRO-05** ✅, **ORDPRO-11** ✅ | **Done (2026-06-11):** guard 04/05 **không đổi** (đã có sẵn) → chỉ phủ test. Mới `CustomerCancelGuardTest` (5: khách-huỷ-delivered 422 + ORDPRO-11 4 nhánh `required_if`/`min:5`) + `AdminWebOrderUpdateGuardTest` (3: chặn lùi + chặn huỷ delivered + forward OK, auth User admin) + bổ sung assert MESSAGE vào `UpdateOrderStatusTest` (API). ORDPRO-11: thêm rule BE `reason: required_if:reason_code,other|min:5|max:500` trong `cancelByCustomer` (KHÔNG over-block lý do preset — `min`/`max` bỏ qua absent/null). Đăng ký 2 file mới vào suite `Zalo`. Xanh: `composer test:zalo` 48 + regression `StockReleaseOnCancelTest` 5. |
| **B5** ✅ | **Refund** — FE `detail.tsx` (`refundStatusInfo`) + BE test | **ORDPRO-09** ✅, **ORDPRO-10** ✅, **ORDPRO-08** ✅ (phần refund) | **Done (2026-06-11):** điều tra xác nhận **logic BE đã đúng** (`RefundService`/`CheckRefundStatus` set `refund_status` chuẩn theo method) → BE chỉ thêm TEST, không đổi prod (giữ MAC/secret). FE `detail.tsx`: thay `REFUND_STATUS_LABEL` (Record phẳng) bằng `refundStatusInfo(order)` key theo CẢ `refundStatus`+`paymentMethod` — `pending_manual` theo method (MoMo ~24h / Bank 2–7 ngày; ZaloPay auto-fail/method lạ → "đang xử lý thủ công, liên hệ hỗ trợ"); `processing` ZaloPay "5–15′"; `not_required` → ẩn dòng. Modal trước-huỷ giữ nguyên (chốt PO). Test: `RefundServiceTest` (10) + `CheckRefundStatusJobTest` (5) xanh; regression `StockReleaseOnCancelTest` (5). FE chưa có hạ tầng test → bảng map nhãn review tay + checklist sandbox. |
| **B6** ✅ | **Hoàn kho khi huỷ** — `StockService::releaseReservation` + tests | **STOCK-06** ✅, **ORDPRO-08** ✅ (phần kho) | **Done:** điều tra xác nhận assert bị bỏ ở `ViettelPostWebhookTest:204-206` là **lỗ hổng fixture, KHÔNG phải bug** (release no-op khi order không có item gắn batch) → chỉ bổ sung test, không sửa logic. Mới `StockReleaseOnCancelTest` (5: khách-huỷ hoàn theo item, admin-huỷ, depleted→active, multi-batch hoàn đúng phần mỗi lô, ORDPRO-08 `refund_status='not_required'`+hoàn kho); **khôi phục assert** hoàn kho ở `ViettelPostWebhookTest` (504 → sold=0/remaining đủ/depleted→active); thêm `CancelUnpaidOrderTest::test_auto_cancel_reverts_depleted_batch_to_active` (nhánh job B1). Xanh: 5+7+13, regression FarmHubTest 29. *Nhãn refund FE để B5.* |
| **B7** ✅ | **Phân quyền payout farm** — `FarmHubController::payouts/payoutDetail` + FE `footer.tsx`/`payouts.tsx` | **ROLE-04** | **Done:** helper `ensureOwner()` đầu `payouts()`+`payoutDetail()` → 403 `'Bạn không có quyền xem mục này'` nếu `!isFarmOwner()`; FE ẩn tab "Thu nhập" + màn chặn khi `!is_owner` (gate cả fetch). Test: +4 case (owner 200, staff list/detail/farm-khác 403) trong `FarmHubTest` (29/29 pass). |
| **B8** ✅ | **Guard/redirect farm** — `EnsureFarmPartner` + FE `layout.tsx` + `farm-access-notice.tsx` (mới) | **ROLE-01** ✅, **ROLE-02** ✅, **ROLE-05** ✅ | **Done:** thêm discriminator `code` vào body 403 (`FARM_SUSPENDED` / `FARM_PARTNER_REQUIRED` / `FARM_NOT_ASSIGNED`) — tái dùng pattern `code:ACCOUNT_DISABLED`, **message giữ nguyên** ở nhánh ROLE-02 (`'Bạn không có quyền…'`). `EnsureFarmPartner`: branch `farm_partner_status==='suspended'` → "Farm của bạn đang tạm dừng…" (ROLE-05); tách lookup farm bằng `Farm::find()` không-scope rồi branch `is_active`/`approved_at` (inactive=SUSPENDED, no-row/approved_at=null=NOT_ASSIGNED). `authenticate` trả thêm `data.user.farm_partner_status`. FE: type `CustomerProfile.farm_partner_status?` + atom `farmPartnerStatusState`; layout.tsx thay silent-redirect bằng **overlay** `FarmAccessNotice` 3 variant (none → mời đăng ký / requested → "Đang chờ duyệt" / suspended → "tạm dừng" + liên hệ admin). Test: FarmHubTest **32** (+3 mới: suspended/inactive-farm/requested; +2 nâng cấp code) qua **`composer test:farm`** (suite `Farm`, KHÔNG phải `Zalo`); AuthenticateTest **6** (+1 `farm_partner_status`) qua `test:zalo`. FE `tsc --noEmit` sạch (chưa có test FE → checklist sandbox). *Lưu ý:* `requestPartnership` vẫn 501 stub (chưa set `requested`) → màn "Đang chờ duyệt" forward-looking; wiring thực sự set requested ngoài scope B8. |
| **B9** ✅ | **Seed dữ liệu** — `database/seeders` | **ORDER-10** | **Done:** tạo `VoucherSeeder` (`GIAM20K` fixed 20.000đ `min_order_amount=100000`, `SALE10`, `FREESHIP`) idempotent + wire `DatabaseSeeder`; test `VoucherSeederTest` (4). Settings `affiliate_*` **đã có** qua migration `2026_05_14_000004` → không seed lại. |
| **B10** ✅ | **Auth re-cấp quyền** — FE `hooks.ts` (`useZaloAuthSync`/`useInitAuth`/`useRequestInformation`) + BE `authenticate` | **AUTH-01** ✅, **AUTH-03** ✅ | **Done:** trích core auth-sync thành hook `useZaloAuthSync()` dùng chung; `useRequestInformation` sau `authorize` thành công gọi `syncAuth({assumeGranted:true})` → re-`getUserInfo` + re-`authenticate` NGAY → tên/ảnh thật hiện trong CHÍNH phiên (AUTH-01), không đợi mở lại app. BE đổi placeholder tạo customer `'Zalo User'` → `'Khách Zalo'` (AUTH-03), khớp FE. Test: `AuthenticateTest` (5: client-name ưu tiên Graph, placeholder "Khách Zalo", idempotent `firebase_id`, re-auth đè placeholder, guard không che tên thật) đăng ký suite `Zalo`. FE `tsc --noEmit` sạch (chưa có hạ tầng test FE → checklist sandbox). |
| **B11** ✅ | **Affiliate referrals/bank + admin-web** — `AffiliateController` + `Admin/AffiliatePartnerController` | **AFF-04** ✅, **AFF-05** ✅ | **Done:** fix `updateBank` dùng `array_key_exists` thay `??` (phân biệt field **vắng** = không đổi vs `""` = xoá). Test: `AffiliateBankAndProfileTest` (6: absent-key / empty-clears / new-value / me-shape 13 key / referrals mask tên+mobile / pagination-meta) + `AffiliateAdminTest` (10: approve/reject/suspend, payout FIFO ×3, toggle commission-rate/auto-approve/enabled). Đăng ký suite `Affiliate` (`composer test:affiliate`). ⚠️ Backend working tree: `AffiliateController.php` + 2 file test **CHƯA commit** (xem Chốt sổ). |
| **B12** ✅ | **FE nhập kho hạn dùng** — `pages/farm/stock-in.tsx`, `import-sheet.tsx` + màn xem lô (`view=batches`) | **STOCK-02** ✅ | **Done (FE + test BE):** `zmp-ui` **DatePicker** hạn dùng (optional, `startDate`=hôm nay chặn quá khứ, `dd/mm/yyyy`, gửi BE `YYYY-MM-DD`) ở **stock-in** (từng dòng, fallback `suggested_expire_date`, bỏ trống→BE auto +5) và **import-sheet** (bỏ trống→lô không hạn). Thêm tab **"Lô hàng"** trong `/farm/movements/:id` dùng hook **`useFarmBatches`** (`usePolling`, `view=batches`) hiển thị `expire_date` từng lô + **cảnh báo màu** (<0=đỏ "Đã hết hạn", 0–6=cam "Sắp hết hạn · còn N ngày"). Helper `toYmd`/`formatYmdDisplay`/`daysUntil` + type `InventoryBatch`. **Bug đã sửa luôn:** `import-sheet` gọi sai route `/inventory/{id}/import` (405) → đổi `POST /inventory/import` (`product_id` trong body). Test BE +3 phủ nhánh client-supplied `expire_date` (FarmHubTest **40 passed**); `tsc --noEmit` sạch. **BE không đổi code.** |
| **B13** ✅ | **Hiển thị tồn/hết hàng** — FE `state.ts` (`productsState`) + `product-detail` + `QuantityInput` | **PROD-05** ✅, **PROD-04** ✅, **STOCK-04** ✅ | **Done (chỉ FE):** **PROD-05** — `flashSaleProductsState`/`recommendedProductsState`/`searchResultState`/`productsByCategoryState` đổi nguồn sang `allProductsState`; `product-item.tsx` thêm badge **"Hết hàng"** (`bg-danger`, grayscale ảnh) + nút disabled; `productsState` giữ nguyên (lookup `useAddToCart` đổi sang `allProductsState`). **STOCK-04** — `quantity-input.tsx` thêm `maxValue` (disable "+" + "Còn lại X"), `ProductItem` & product-detail stepper cap theo `stockAvailable`; **giữ BE 422**, FE hiện **nguyên văn `parsed.message`** + shortages thô. **PROD-04** — chỉ ghi chú điều kiện (tạo→gắn farm→nhập lô active), không đổi logic. `tsc --noEmit` pass. |
| **B14** ✅ | **Dashboard farm** — `FarmDashboardService` + `FarmHubController` + FE `pages/farm/index.tsx` | **HUB-01** ✅ | **Done:** tách 2 chỉ số song song — **Đã đặt hôm nay** (`placedBaseQuery`: `created_at` hôm nay VN-tz, mọi status trừ `cancelled`) / **Đã giao hôm nay** (`itemsBaseQuery`: `delivered_at`, delivered). `getOverview` trả thêm khối `placed`/`delivered` (top-level giữ basis "đã giao" cho backward-compat); `productsToday` trả `products_placed`/`products_delivered` (AI hint trên `placed`). FE: 2 tab Đặt/Giao → card + list đổi đồng bộ cùng basis. Test BE **37 passed** (3 overview + 2 products-today mới, giữ xanh test cũ); `tsc --noEmit` sạch. |
| **B15** ✅ | **Màn chi tiết đóng gói** — `FarmPackingController@show` + FE route `/farm/orders/:id` | **PACK-07** ✅ | **Done:** route `/farm/orders/:id` đã có sẵn ở BE; **enrich** `show()` (+`station_name` key riêng, `order_total`, `assigned_customer_name`, `packing_started_at`/`packed_at`) giữ nguyên masking + pickup `delivery_address = station_name`. FE: list page → `pages/farm/orders/index.tsx`, helper chung tách `orders/_shared.tsx` (`recipientLocation`/`statusPill`/`useAction`/…); màn chi tiết mới `orders/detail.tsx` + hook `useFarmOrderDetail` + type `FarmOrderDetail` + route `router.tsx`. List card bấm → mở chi tiết (`navigate`, nút thao tác `stopPropagation`); pickup hiện **tên trạm** thay " · Pickup". Detail: người nhận (SĐT/địa chỉ ĐÃ CHE; pickup "Nhận tại trạm: …"), items, trạng thái + nút thao tác theo vai trò; farm thường read-only. Test BE **26 passed** (4 mới cho `show()`); `tsc --noEmit` sạch. |
| **B16** ✅ | **Test nhánh listener VTP** — `Listeners/CreateVtpOrderOnPayment` | **ORDER-16** ✅ | **Done:** `CreateVtpOrderOnPaymentListenerTest` (6 test): BANK/MOMO shipping → VTP tạo; pickup/COD → skip; idempotent guard; VTP fail → listener bắt. Suite VtpOrder 23 passed. B2 không xung đột (listener VTP on `OrderPaymentSucceeded`, commission on `OrderDelivered`). |
| **B17** ✅ | **Test idempotency checkout + fix TOCTOU** — `ZaloApiController::store` + migration `cache_locks` | **ORDER-08** ✅ | **Done:** đóng window TOCTOU (giữa `Cache::get` và `Cache::put` 2 request đồng thời đều miss → lọt 2 đơn) bằng **atomic database lock**: bọc re-check→tạo đơn→`Cache::put` trong `Cache::store('database')->lock($lockKey,10)->block(3)` (release `finally`); fast-path `Cache::get` ngoài lock; helper `respondDuplicatedOrder()` (DRY check). Production `CACHE_STORE=file` không hỗ trợ lock → cố tình dùng `database` store + **migration mới** `2026_06_12_000001_create_cache_locks_table`; lock-timeout 3s → đi tiếp idempotency-an-toàn. Test: `CheckoutIdempotencyTest` (3: 2× trùng → 1 đơn + `duplicated:true`+orderId cũ; cached short-circuit; payload khác → 2 đơn) đăng ký suite `Zalo`. `composer test:zalo` **52 xanh**. |
| **B18** ✅ | **TZ farm-dashboard "hôm nay"** — `FarmDashboardService` + `FarmHubController@productsToday` + `FarmStockController@suggestions` | **HUB-01** (follow-up re-audit) | **Done:** phát hiện khi re-audit B14 — query `whereBetween` sau `->setTimezone('UTC')` vì tưởng `created_at`/`delivered_at` lưu UTC, nhưng production lưu **giờ VN** (`app.timezone=Asia/Ho_Chi_Minh`, cột `dateTime`, `delivered_at=now()`, `created_at` FE gửi `+07:00`) → cửa sổ "hôm nay" lệch **-7h** (đơn sau ~07:00 sáng VN rớt; đơn tối hôm qua lọt vào). Test B14 cũ che bug vì fixture ghi UTC. **Sửa:** bỏ `setTimezone('UTC')`/`CONVERT_TZ`, so giờ VN trực tiếp (5 chỗ: `placedBaseQuery`, `itemsBaseQuery`, `getRevenueTimeseries`, `productsToday`, `suggestions`); fixture test → giờ VN + 2 case `setTestNow` red→green. `FarmHubTest` **42 passed**; FE không đổi (`tsc` sạch). |
| **B19** ✅ | **Bắt mã giới thiệu bền vững** — FE `src/app.ts` + `utils/affiliate.ts` | **AFF-02** ✅ | **Done (chỉ FE):** **AFF-02** — chống Zalo strip `?ref=` khi mở share link (URL bị rewrite `/zapps/${APP_ID}`): thêm `readRefFromSources()` đọc mã ưu tiên **`getRouteParams().ref`** (kênh tham số chính thức ZMP, sync, minVersion 2.11.0) → fallback `window.location.search ?ref=`. Gom thành `captureRefCode()` (thay block inline `app.ts`), normalize `trim().toUpperCase()`, **first-capture-wins** (không ghi đè mã đã bắt). `applyPendingReferral`: thêm guard module-level (`referralAppliedThisSession`/`referralApplyInFlight`) → 5 điểm gọi đồng thời gom 1 POST, deterministic→latch, **lỗi mạng KHÔNG latch** (giữ retry thật); BE 409 backstop. **Không ghi đè referrer cũ** đã do BE đảm bảo (`applyReferral` 409). BE không đổi. `tsc --noEmit` sạch. **FE không có hạ tầng test → checklist verify thiết bị thật trong `aff.md`.** |

---

## Tổng quan ưu tiên

- **Cao (sửa trước):** ~~ORDER-06 + ORDER-04 (B1)~~ ✅, ~~ROLE-04 (B7)~~ ✅, ~~AFF-03 (B2)~~ ✅. → rủi ro tiền/kho/bảo mật & sẽ sai khi lên production.
- **Trung bình:** ~~AUTH-01 (B10)~~ ✅, ORDER-10 (B9) ✅, ~~ORDPRO-08/09/10 (B5/B6)~~ ✅, ~~STOCK-06 (B6)~~ ✅, ~~STOCK-02 (B12)~~ ✅, ~~HUB-01 (B14)~~ ✅, ~~PROD-01 (B3)~~ ✅, ~~PACK-07 (B15)~~ ✅, ~~AFF-02 (B19)~~ ✅, ~~ORDER-08 (B17)~~ ✅.
- **Thấp:** ~~PROD-02/03 (B3)~~ ✅, ~~ROLE-01/02/05 (B8)~~ ✅, ~~ORDPRO-04/05/11 (B4)~~ ✅, ~~ORDER-16 (B16)~~ ✅, **ORDER-03 — TREO** (xem Chốt sổ), ~~STOCK-04 + PROD-04/05 (B13)~~ ✅, ~~AFF-04/05 (B11)~~ ✅, ~~AUTH-03 (B10)~~ ✅.

> **Theo quy trình BẮT BUỘC (CLAUDE.md):** mọi batch phải **lập plan chi tiết + CHỜ DUYỆT** trước khi sửa, **viết test trước**, rồi sửa từng bước nhỏ. Các case gắn ✅ đã chốt hướng (xem mục **"Quyết định đã chốt"**) — sẵn sàng vào bước lập plan/viết test.

---

## Chốt sổ (2026-06-13)

**19/19 batch (B1–B19) đã hoàn tất** — dòng B11 trước đây thiếu tick ✅ trong bảng (đã vá; thực tế đã xong, có `AffiliateBankAndProfileTest` + `AffiliateAdminTest`).

### Test backend (chạy 2026-06-13)
- `php -d memory_limit=2G ./vendor/bin/phpunit --testsuite Unit,Feature`: **311 tests, 933 assertions — 1 error + 3 failures**.
- *(Lưu ý:* `composer test` / `php artisan test` bare **OOM ở 128MB** — `-d memory_limit` KHÔNG truyền sang tiến trình con `artisan test` spawn; phải gọi **phpunit trực tiếp** với memory flag.)*
- **4 fail đều ở `tests/Feature/OrderCreationWithShippingTest`** (`test_order_created_with_shipping_fields`, `test_order_accepted_when_total_within_1000_tolerance`, `test_pickup_order_with_zero_shipping_fee` → 422 thay 201; `test_delivery_stores_vtp_address_ids` → null `province_id`). **PRE-EXISTING**, KHÔNG phải regression: file không đổi từ commit shipping `f1b9644b` (ngoài B1–B19); đã ghi nhận ở ghi chú B3 (xác nhận clean-tree). Nguyên nhân: fixture gửi VTP address id cứng (province 1/ward 100) mà KHÔNG seed `vtp_locations` → checkout 422 validation.
- **Coverage KHÔNG đo được:** môi trường (PHP 8.5.2) **chưa cài Xdebug/PCOV** → `composer test:coverage --min=80` lỗi ngay khi khởi động. Cần `pecl install pcov` (hoặc bật Xdebug) trước khi đo % theo ngưỡng 80%.

### Còn treo
- **ORDER-03 (TREO — quyết định PO #6 chưa implement):** "Thêm nhãn chữ map 6 status BE → tiếng Việt ở `detail.tsx` + list đơn khách" được **chốt** nhưng **không batch nào gánh**. FE hiện chỉ có nhãn tab + nhãn refund; `pages/orders/detail.tsx` KHÔNG render nhãn trạng thái đơn dạng chữ cho khách. INDEX để "🟢 Đạt (lệch wording)" — chấp nhận tạm. **Cần quyết:** làm 1 batch nhỏ thêm nhãn, hoặc chốt bỏ qua (giữ "Đạt").
- **11 case ⚪ "Chưa kiểm tra" (audit code OK, KHÔNG sai lệch — không phải blocker, chỉ cần test tay):** AUTH-05, ROLE-03, ROLE-06, STOCK-01, STOCK-03, STOCK-05, STOCK-07, ORDPRO-01, ORDPRO-02, ORDPRO-03, ORDPRO-06.
- **AFF-02 (B19):** code xong, **chờ verify thiết bị thật** (deep link `?ref=` — checklist trong `aff.md` & `MANUAL-TEST.md`).

### Việc cần làm để đóng sổ
1. **Commit backend B11/B16:** working tree backend còn `AffiliateController.php` (M), `phpunit.xml` (M), `AffiliateAdminTest.php`/`AffiliateBankAndProfileTest.php`/`CreateVtpOrderOnPaymentListenerTest.php` (untracked) **chưa commit** — phải commit kẻo mất khi pull/reset.
2. **Test tay:** chạy `docs/use-cases/MANUAL-TEST.md` (66 case + cutover production) trên Zalo sandbox/thiết bị thật.
3. **Production cutover:** xem section 9 trong `MANUAL-TEST.md` + `zalo_production_test_plan.md` Phase 8–9 (đổi `COD_SANDBOX→COD`/`BANK_SANDBOX→BANK`, secrets production).
4. *(Tuỳ chọn)* cài PCOV/Xdebug rồi chạy lại `composer test:coverage` để có % theo ngưỡng 80%.
